# libraries
from enum import Enum


class User(Enum):
    """
    class derived from Enum-Class
    holding user credentials for database access
    """

    NAME = 
    PASSWORD = 


class Database(Enum):
    """
    class derived from Enum-Class
    holding database credentials required for routing to the database system
    service
    """

    NAME = 
    PORT = 
    SID =



